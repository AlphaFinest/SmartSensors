@model IEnumerable<FullSensorViewModel>
@{
    ViewBag.Title = "AllSensors";
}
<h3>All Sensors</h3>
<table id="myTable" class="table table-striped table-dark table-bordered table-hover">
    <thead>
        <tr>
            <th>Owner</th>
            <th>Sensor Name</th>
            <th>Description</th>
            <th>Value</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var m in Model)
            {
            <tr>
                <td>@m.Owner</td>
                <td>@m.Name</td>
                <td>@m.Description</td>
                @if (m.ValueType == "(true/false)")
                {
                    <td>@m.Value</td>
                }
                else
                {
                    <td>@m.Value @m.ValueType</td>
                }

                <td>@Html.ActionLink("Edit", "EditSensor", new { sensor = m.Name })</td>
            </tr>
        }
    </tbody>
</table>
@section grid
        {
    <script type="text/javascript" src="http://cdn.datatables.net/1.10.2/js/jquery.dataTables.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#myTable').DataTable();
        });
    </script>

}

